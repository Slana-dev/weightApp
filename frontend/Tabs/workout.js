
import React from "react";
import { View, Text } from "react-native";

export default function WorkoutScreen() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: "rgba(93, 111, 173, 1)" }}>
            <Text style={{ color: "white", fontSize: 24 }}>Workout Screen</Text>
        </View>
    );
}